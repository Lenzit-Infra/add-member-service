# Used only for the production build (npm run build), picked up by Vite automatically.
# Point this at wherever the Cloudflare Tunnel exposes the backend.
VITE_API_BASE_URL=https://api.lenzit.ir/api/v1
